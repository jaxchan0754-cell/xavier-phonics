{
  "name": "xavier-phonics",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Xavier 英语自然拼读 PWA（M1 工程骨架）",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "gen:icons": "node scripts/generate-icons.mjs",
    "gen:tts": "node scripts/generate-tts.mjs",
    "test": "esbuild tests/*.test.ts --bundle --format=esm --platform=node --external:@huggingface/transformers --outdir=node_modules/.tmp-tests --log-level=warning && for f in node_modules/.tmp-tests/*.test.js; do node \"$f\" || exit 1; done"
  },
  "devDependencies": {
    "fake-indexeddb": "^6.2.5",
    "typescript": "^5.6.0",
    "vite": "^6.0.0",
    "vite-plugin-pwa": "^0.21.0"
  },
  "dependencies": {
    "@huggingface/transformers": "^4.2.0",
    "dexie": "^4.0.0"
  }
}
